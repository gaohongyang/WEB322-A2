<div class="dashBoard container">
    <div class="row">
        {{> userDashboardNav}}

        <div class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
            <div
                class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
                <h1 class="h2 text-white">Rent List</h1>
            </div>
            <div class="text-white">
                <table class="container table table-dark table-hover table-bordered w-75">
                    <thead>
                        <tr>
                            <th class="w-25 text-center" scope="col">Title</th>
                            <th class="text-center" scope="col">Price</th>
                            <th class="text-center" scope="col">Location</th>
                            <th class="text-center" scope="col">Time</th>
                            <th class="text-center">Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each filteredRent}}
                        <tr>
                            <td class="text-center pt-4">{{this.title}}</td>
                            <td class="text-center pt-4">{{this.rent}}</td>
                            <td class="text-center pt-4">{{this.location}}</td>
                            <td class="text-center pt-4">{{this.time}}</td>
                            <td class="text-center">
                                <a href="/deleteRent/{{this._id}}?method=DELETE">
                                    <button class="btn btn-danger badge-pill" style="width:80px;">Delete</button>
                                </a>
                            </td>
                        </tr>
                        {{else}}
                        <td colspan="5">
                            <p>No product in your rent cart</p>
                        </td>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>